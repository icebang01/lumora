# 黑胶唱机视觉微调

## 完成内容
- 删除经典黑胶（`style-vinyl`）样式中唱片中心的白色主轴点（`.ms-spindle`）。
- 删除唱片中心的默认音符占位 SVG（`.ms-note`）及其样式。
- 将唱臂 `.ms-tonearm` 移入 `.ms-turntable-base` 内部，使其相对于唱机底座定位。
- 唱头（`::after`）恢复为 14x18px 矩形圆角；支点圆盘（`::before`）保持 18px 圆形小球。
- 调整支点 `right: 3.5%`、角度 `rotate(176deg)`，让唱头落在唱片上边缘中央，支点落在白色外壳右上角。
- **以唱片大圆为圆心旋转 180°（初版）**：把唱臂整体镜像到左下——定位由 `top/right` 改为 `left:3.5%; bottom:10%`，`transform-origin` 由 `92% 50%` 翻为 `8% 50%`，角度由 `176deg` 改为 `356deg`（180°+176°）；支点 `::before` 落在白色外壳左下角，唱头 `::after` 落在唱片下边缘。
- **修正为水平镜像**：用户反馈初版（左下）不是预期效果，改为支点在**白色外壳左上角**、唱头压在**唱片上边缘左侧**的水平镜像方案。定位改回 `top:10%; left:3.5%`，`transform-origin` 保持 `8% 50%`，角度由 `356deg` 改为 `4deg`。
- **最终回到右上角**：用户反馈截图希望唱臂仍在**右上角**，因此恢复为最初的目标位置：支点在白色外壳右上角，唱头压在唱片上边缘。定位 `top:10%; right:3.5%`，`transform-origin:92% 50%`，角度 `rotate(176deg)`。
- **修正唱臂角度**：用户反馈 `rotate(176deg)` 让唱臂甩到右侧外侧、没有压在唱片上。改为 `rotate(20deg)`，使唱臂从右上角支点向左下倾斜，方形唱头真正落在唱片上边缘。
- **播放/暂停唱臂动画**：以用户截图中当前位置为**暂停位**（`rotate(20deg)`，唱头悬停/轻触唱片上边缘）；播放时唱臂再顺时针落下约 18° 到 `rotate(38deg)`，让唱头切实压在黑色唱片上（箭头所指位置）。
- **增大播放落针角度到 55°**：用户反馈 `rotate(38deg)` 仍没真正压在黑色唱片上（"你管这叫黑色唱片？"）。将播放位从 `rotate(38deg)` 增大到 `rotate(55deg)`，唱臂顺时针落下更多、方形唱头更深入地压在黑色唱片上。暂停位保持 `rotate(20deg)` 不变。
- **精确落点到箭头位置**：用户截图标注黑色唱片左上区域（约 10–11 点钟方向），要求唱头精确落到箭头处。将播放位从 `rotate(55deg)` 回调到 `rotate(47deg)`，使方形唱头对准箭头所指位置。
- **按用户指定角度设为 65°**：用户进一步要求「往下 65 度」，直接将播放位从 `rotate(47deg)` 改为 `rotate(65deg)`，让唱头落到黑色唱片上边缘箭头所指处。
- **修正为 -65°**：用户纠正「我说的是 -65 度」，正 65° 反而越跑越远。将播放位从 `rotate(65deg)` 改为 `rotate(-65deg)`，让唱头回到目标位置。
- **再次改为 +40°**：用户最新指令「现在 +40 度」，将播放位从 `rotate(-65deg)` 改为 `rotate(40deg)`。
- **最终改为 -20°**：用户最终指令「还是 -20 度吧」，将播放位从 `rotate(40deg)` 改为 `rotate(-20deg)`。
- **微调为 -15°**：用户最终微调指令「还是 -15 度吧」，将播放位从 `rotate(-20deg)` 改为 `rotate(-15deg)`。
- **收窄银色转盘外框**：用户截图箭头指向银色金属环，要求收窄。将黑胶唱片 `.ms-vinyl` 从 `74%` 放大到 `78%`（定位从 `13%` 调整到 `11%`），使银色环宽度从约 5% 缩到约 3%。
- **增强整体立体感**：用户反馈「太平面」，对底座/转盘/黑胶/封面四层同时加深阴影、强化高光与内凹效果：
  - 底座：加大外投阴影层次，补充侧向高光与内阴影，强化圆角方块体积感。
  - 底座内凹区：加深内阴影、增强顶部反光与边缘高光。
  - 银色转盘：强化金属径向渐变，增加外沿投影与双层内阴影，做出凹陷金属环效果。
  - 黑胶唱片：增加外投阴影、内边缘高光与暗边，让唱片在转盘中更显凸起/内嵌。
  - 中心封面：加深内阴影与外投阴影，增强标签嵌入感。
- **修复所有播放器样式不显示专辑封面**：根因是 `.ms-cover` 被嵌套在 `.ms-turntable-base` 内部，而非黑胶样式下 `.ms-turntable-base` 为 `display:none`，导致封面被一起隐藏；黑胶样式下同样未渲染。修复：把 `.ms-cover` 移出唱机底座作为 `.ms-art` 的直接子元素（唯一必要 DOM 改动）。为保持黑胶唱机视觉大小不变，vinyl 封面由 `46%`（相对于 78% 的黑胶）改为 `36%`（相对于 `.ms-art`，0.46×0.78≈0.36），且不添加旋转动画，保持封面标签静止。6ab1787 曾因擅自加入旋转/缩尺寸破坏排版而被撤销；本次在用户确认截图 bug 后，用最小化方案重新修复。
- **修复封面修复导致的歌词排版错乱**：用户截图显示歌词跑到了黑胶上方。排查发现 `index.html` 中 `.ms-content` 提前关闭，`.ms-right` 被甩到 `#music-stage` 的 grid 外部成为独立 grid item，被自动放到主内容区上方。修复：把 `.ms-content` 的闭合标签移回 `.ms-right` 之后，恢复横向双栏布局。该问题在更早的 index.html 中已存在，因本次改动才暴露。
- **删除简约方形 / 大封面样式底部的音频频谱**：用户截图箭头指向 `style-square`（简约方形）与 `style-cover`（大封面）封面底部的 FFT 频谱条（`#m-spectrum` canvas），要求删除。该频谱隐藏规则原本只覆盖 `lyrics-min/lyrics/vinyl` 三种样式，扩展为同时覆盖 `square` 与 `cover`。纯 CSS 改动（`display:none !important`），JS 频谱逻辑未动。
- **修复唱臂支点圆球超出白色唱机底座**：用户截图显示右上角支点圆球冒出圆角外壳。根因是 `right:3.5%` 让唱臂右端几乎贴在圆角边界上，加上 18px 支点圆球又向右侧探出 8px。首次修复（`180bc6e`）内缩到 `right:7%`、圆球 14px/-2px，但用户仍看到旧效果——因为 `index.html` 里 `style.css?v=50` 被 Chromium 缓存，未加载新样式。二次修复（`4be72df`）：唱臂进一步内缩到 `right:9%`、宽度 `46%`；支点圆球改为 12px 且 `right:0`（圆心正好在唱臂末端，不再向外探出）；并把 `style.css` 缓存版本号从 `?v=50` 提升到 `?v=51`，强制渲染端重新加载样式表。暂停/播放角度维持 `20deg / -15deg` 不变。
- **第四轮修复——唱臂仍超出底座（用户第四次反馈）**：前三次几何内缩+缓存 bust 仍没能让用户看到变化（Chromium 缓存 `file://` 的 `index.html` 本身，缓存 bust 不够彻底）。本轮用「双保险」彻底解决（`f320dbb`）：① 视觉兜底——`.ms-turntable-base` 加 `overflow: hidden`，物理裁剪任何超出圆角（border-radius:22%）的子元素（唱臂/支点圆球），无论几何多接近边界都不会再探出白色外壳；② 几何再内缩——`.ms-tonearm` `top:10%→12%`、`right:9%→16%`、`width:46%→36%`，支点 `::before` 圆球 `12px→10px`、`right:0`；③ 根治缓存——`src/main/windows.js` 的 `win.loadFile(...index.html, { search:'v=51' })` 改为 `win.loadURL('file://'+...index.html+'?v='+Date.now(), { extraHeaders: 'pragma: no-cache\ncache-control: no-cache\n' })`，每次启动 URL 带唯一时间戳+no-cache 头，Chromium 绝不用旧缓存；④ 两个启动 bat 新增清理 `%APPDATA%\electron\Cache` 与 `%LOCALAPPDATA%\Lumora\Cache`。暂停/播放角度维持 `20deg / -15deg` 不变。

## 关键决策
- 把唱臂从 `.ms-content` 的兄弟节点改为 `.ms-turntable-base` 的子节点，百分比定位现在基于底座本身，不同窗口尺寸下支点/唱头与唱机的相对位置更稳定。
- 删除主轴和音符占位时同步移除其 HTML 与 CSS，不再保留隐藏态。
- 唱头恢复为方形（`border-radius: 4px`），不再改为圆形。
- 唱臂最终位置在**右上角**，支点在白色外壳右上角，唱头压在唱片上边缘。角度从错误的 `176deg` 修正为 `20deg`（顺时针向下倾斜），让唱臂真正搭在唱片上。
- **暂停 vs 播放**：用 CSS 类 `.playing` 区分两种角度；暂停 `20deg`，播放 `-15deg`（按用户最终微调指令设定，让唱头落到黑色唱片上边缘箭头位置），过渡动画保持 `.85s var(--ease)`，模拟真实唱机落针。

## 验证
- `npm run lint:syntax`、`npm run lint:imports`、`npm run test:unit`、`npm run typecheck` 全过。
- pre-commit 钩子通过（check-syntax/scan-reverse-leaks/audit-imports/CRLF）。
- commit `91a1af2`（删除主轴/移动唱臂）、`4d300e7`（支点/唱头球形优化）、`ecfc3fc`（删除音符/恢复方形唱头）、`9ba8571`（初版 180° 旋转）、`536ace8`（修正为左上水平镜像）、`fa00f49`（还原到右上角）、`e8041c3`（修正唱臂角度到 20deg）、`11bac52`（播放/暂停唱臂动画 38°）、`f555476`（播放落针增大到 55°）、`a2f35dd`（精确落到箭头位置 47°）、`7149c6e`（用户指定正 65°，错误）、`e0f2c6f`（修正为 -65°）、`50fc6c2`（用户最新 +40°）、`542daae`（最终 -20°）、`992e8bb`（微调 -15°）、`2e3c293`（收窄银色转盘外框）、`98d943d`（增强整体立体感）已提交。`6ab1787`（修复封面显示，含旋转动画）**曾被撤销** → revert `776e34f`；后按用户截图确认的最小化方案重新修复 → `cc964f2`（封面移出底座、vinyl 36% 无旋转）；紧接着修复因此暴露的 `.ms-content` 提前闭合 → `81f1732`。index.html/style.css 经 Contents API 镜像推送（remote blobs `4791f8d`/`95b6d48`）。远端因沙箱 git 碎片化，完整历史待 `git push --force origin main` 对齐。
- 频谱删除（`2bfb3fe`）已提交；style.css 经 Contents API 镜像推送（remote blob `0bfdd29`）。
- 唱臂超出底座修复（`180bc6e`）已提交；style.css 经 Contents API 镜像推送（remote blob `02f9d39`）。
- 二次修复（含缓存版本号提升 `?v=50→?v=51` 与更激进内缩）commit `4be72df` 已提交；index.html/style.css 经 Contents API 镜像推送（remote blobs `6049426` / `e2d1d8c`）。
- 三次修复：发现 Chromium 会缓存 `file://` 协议的 `index.html` 本身，导致 HTML 内的 `style.css?v=51` 也走旧缓存。在 `src/main/windows.js` 的 `win.loadFile` 上追加 `{ search: 'v=51' }`，让窗口 URL 变成 `index.html?v=51`，从而强制重新加载 HTML 与 CSS。commit `963f9aa` 已提交；windows.js 经 Contents API 镜像推送（remote blob `543b79d`）。
- 第四轮修复（用户第四次反馈仍超底座）：单靠 CSS 内缩 + 版本号已无法保证用户看到更新，根因是 Chromium 把 `file://` 的 `index.html` 本身也缓存了。本轮双保险（`f320dbb`）：① `.ms-turntable-base` 加 `overflow:hidden` 物理裁剪超出圆角的唱臂/支点；② 唱臂再内缩 `top:12% right:16% width:36%`、支点圆球 `10px`；③ `src/main/windows.js` 改 `win.loadURL(file://...index.html?v=Date.now())` + `no-cache` 头，彻底禁用 file:// 缓存；④ 两个启动 bat 清 `%APPDATA%\electron\Cache`、`%LOCALAPPDATA%\Lumora\Cache`。已提交；style.css→`1cf2a08`、windows.js→`1667c12`、fix-and-start.bat→`aa805fb`、一键修复并启动.bat→`7fb4673` 经 Contents API 镜像推送。验证门禁全过（lint:syntax 145/145、test:unit 111/111、typecheck、pre-commit）。
- 第五轮微调（用户反馈支点圆球被裁剪）：`overflow:hidden` 兜底后，支点圆球顶部被底座 `border-radius:22%` 的圆角边界裁剪。将 `.ms-tonearm` `top:12% → 15%`，唱臂整体下移 3%，支点脱离顶部圆角危险区。其他参数不变。commit `00addf3` 已提交；style.css 经 Contents API 镜像推送（remote blob `8d25cf2`）。
- 配色调整（用户反馈播放按钮/进度条颜色未跟随播放器样式）：vinyl 样式底部播放按钮原硬编码蓝色渐变 `#4d8dff → #3a6df0`，进度条 fallback 为 `#2b6cf0`。改为使用 Lumora 品牌强调色：按钮背景 `linear-gradient(135deg, var(--accent-cyan) 0%, var(--accent) 55%, var(--accent-pink) 100%)`（青 `#6ee7ff` → 紫 `#7c8cff` → 粉 `#ff7ac6`），按钮阴影 `var(--accent-glow)`；进度条/拖动点 fallback 改为 `var(--accent)`。`--album-accent`（专辑封面主色）仍优先驱动进度条，无封面主色时统一用品牌紫。commit `4050938` 已提交；style.css 经 Contents API 镜像推送（remote blob `6f94fad`）。
- 全样式自适应（用户要求"每一个播放器样式"都自适应颜色）：默认 `.mc-btn--primary` 原硬编码网易云蓝 `#6ea8ff → #4d8dff → #3a6df0`；`mc-theme-light` 的播放按钮原硬编码金黄色 `#ffd84d → #ffc400 → #ffb300`；进度条拖动点 `#m-seek-handle` 默认白色、`mc-theme-light` 下黑色。统一改造为：所有音乐样式（lyrics / lyrics-min / square / cover / vinyl / glass）的播放按钮底层使用 `var(--album-accent, var(--accent, #7c8cff))`，叠加高光/阴影渐变做出立体感；进度条/拖动点全部跟随 `--album-accent`（无专辑色时 fallback 到 `--accent`）。删除 vinyl 单独覆盖，让 vinyl 也走统一规则。commit `f36645e` 已提交；style.css 经 Contents API 镜像推送（remote blob `15fa55a`）。
- **纠正"所有样式颜色雷同"（用户反馈"按理说每个样式里这两个的颜色都不一样"）**：上一步让所有样式都读 `var(--album-accent, var(--accent))`，结果无专辑封面主色时一律 fallback 到同一个品牌紫 `--accent #7c8cff`，导致各样式的播放按钮/进度条看起来全是同一种紫——用户真正要的是**每个样式有各自固定的主题色**，而非跟随专辑封面。修复（`66dfc51`）：在 `style.css` 新增每样式 `--style-accent` 变量块（`#music-stage.style-xxx { --style-accent: <独立色> }`），配色方案为 default=品牌紫 `#7c8cff` / cover=专辑色（`var(--album-accent, #ff7ac6)`，保留跟随封面）/ lyrics=青 `#6ee7ff` / minimal=紫 `#a78bfa` / vinyl=橙 `#ff9f43` / square=青绿 `#4ecdc4` / glass=蓝 `#93c5fd` / lyrics-min=粉 `#ff7ac6`；把 vinyl `#m-seek-progress`/`#m-seek-handle`、`mc-theme-light` 的 `#m-seek-progress`/`#m-seek-handle`/`.mc-btn--primary`、默认 `.mc-btn--primary`、通用 `#m-seek-progress`/`#m-seek-handle` 的取色从 `var(--album-accent, var(--accent))` 改为 `var(--style-accent[, 带 fallback])`。选择器改用 `#music-stage.style-xxx`（而非 `body.music-style-xxx`），因为 JS 只给 body 打了 glass/vinyl 两个标记，其余样式只能靠 `#music-stage` 的 `style-xxx` 类区分。commit `66dfc51` 已提交；style.css 经 Contents API 镜像推送（remote blob `5317137`）。
- **修复 --style-accent 未生效（用户截图反馈"你管这叫改了颜色？"）**：`66dfc51` 把 `--style-accent` 定义在 `#music-stage.style-xxx` 上，但 `#music-controls` 是 `#music-stage` 的兄弟节点，CSS 自定义属性只向子元素继承，无法跨兄弟节点传递，导致底部播放按钮/进度条仍 fallback 到同一品牌紫。修复（`2c97d90`）：① `src/renderer/ui/music-stage.js` 的 `_applyPlayerStyle` 给 `body` 也打上当前样式的 `music-style-<name>` 类（之前只有 glass/vinyl），让 `body` 成为 `#music-stage` 与 `#music-controls` 的共同祖先；② `style.css` 把 `--style-accent` 变量块从 `#music-stage.style-xxx` 迁移到 `body.music-style-xxx`，并同步保留原有 glass/vinyl 的 body 类逻辑。颜色方案不变。commit `2c97d90` 已提交；style.css→`61c8dd7`、music-stage.js→`106f8f8` 经 Contents API 镜像推送。
- **修复音乐舞台底部死黑（用户截图指出"底下黑色的删掉"）**：深色专辑封面经 `.ms-backdrop` 的 `blur(120px) brightness(.65)` 模糊后，底部容易形成一块死黑区域。修复（`5233ad0`）：在 `#music-stage` 新增 `::after` 伪元素作为底部提亮层，在底部 35% 区域叠加 `linear-gradient(to top, rgba(255,255,255,.10) 0%, transparent 35%)`，把死黑稍微提起来，同时保持整体沉浸感；浅色主题 vinyl/glass 通过 `--ms-bottom-lift: none` 关闭提亮，避免白上加白。commit `5233ad0` 已提交；style.css 经 Contents API 镜像推送（remote blob `97e0ab3`）。
- **修复 vinyl 样式右栏空白（用户截图询问"这种空的是什么？"）**：vinyl 样式右栏（`.ms-right`）在内容较少时，顶部出现一块空白。根因是 `.ms-lyrics` 为歌词滚动居中预留的大内边距（`padding: var(--lyrics-pad, 180px)`，由 JS 按 wrapper 高度一半动态设置）叠加 `.ms-right` 的 `justify-content: center` 垂直居中，再加上被 `[hidden]` 隐藏的 `.ms-lyrics-credits` 块，使实际可见内容上方留出大片空白。修复（`957d704`）：① `style-vinyl .ms-right` 由 `justify-content: center` 改为 `flex-start`（内容顶部对齐，内容少时不再把可见内容推到中间留白），`gap: 14px → 10px` 让信息区与歌词区在 credits 隐藏时更紧凑；② 新增 `style-vinyl .ms-lyrics { padding-top/bottom: 18px !important }`，收掉歌词区为滚动居中预留的大内边距，首句上方空白消失。验证门禁全过（lint:syntax 145/145、lint:imports OK、test:unit 111/111、typecheck OK）。commit `957d704` 已提交；style.css 经 Contents API 镜像推送（remote blob `d764e27`）。
- **修复 square 样式右栏信息显示不全（用户截图指出红框处"显示不全"）**：square 样式右栏沿用 base `.ms-right` 的 `justify-content: center`，内容较多时 `.ms-right` 的 `overflow: hidden` 会把上下内容一起裁掉，credits/歌词显示不全；内容少时又因垂直居中在顶部留下空白。修复（`ab82e7e`）：给 `style-square .ms-right` 加 `justify-content: flex-start`，让信息区/credits/歌词区从顶部开始排列，歌词区可充分利用 `.ms-right` 的可用高度，避免上下被裁。验证门禁全过（lint:syntax 145/145、lint:imports OK、test:unit 111/111、typecheck OK）。commit `ab82e7e` 已提交；style.css 经 Contents API 镜像推送（remote blob `83a85dd`）。
- **修复 vinyl 样式歌词底部看不清（用户截图指出歌词区底部"看不清"）**：vinyl 为浅色主题，未唱歌词原色 `rgba(0,0,0,.46)` 在白色背景下对比度偏低；歌词区上下又有 `transparent→#000 16% / #000 84%→transparent` 的淡出遮罩，把接近上下边缘的歌词进一步虚化，导致底部出现看不清的虚影/空白区。修复（`665d7a9`）：① 未唱歌词颜色从 `.46` 加深到 `.60`，hover 从 `.78` 提到 `.84`；② 当前行未高亮段也同步用 `.60`；③ 歌词区遮罩收窄为 `transparent 0%→#000 6% / #000 94%→transparent 100%`，边缘歌词不再被吃掉。验证门禁全过（lint:syntax 145/145、lint:imports OK、test:unit 111/111、typecheck OK）。commit `665d7a9` 已提交；style.css 经 Contents API 镜像推送（remote blob `e712f3b`）。
- **修复 vinyl 样式歌词偏移工具条看不清（用户截图指出工具条 -5s/-0.5s/+0.5s/+5s 按钮"看不清"）**：vinyl 为浅色主题，歌词偏移工具条 `.ms-lyrics-bar` 内的按钮 `.mlo-btn` 全局用白色文字/边框（为暗色主题设计），在 vinyl 白色背景下几乎隐形。修复（`7231eca`）：① 新增 vinyl 专属覆盖，`.ms-lyrics-wrap.has-lyrics .ms-lyrics-bar` 默认 `opacity: .55` 且 `pointer-events: auto`（从 base 的 `opacity:0` 仅 hover 揭示改为默认半透明显示，保证可发现），hover/focus-within 时 `opacity: 1` 全亮；② `.mlo-btn` 改深色系——文字 `rgba(0,0,0,.62)`、背景 `rgba(0,0,0,.06)`、边框 `rgba(0,0,0,.14)`，hover 加深到文字 `#111827`/背景 `rgba(0,0,0,.12)`，`[aria-pressed="true"]` 按下态用青色 `rgba(8,145,178,.75)`/边框 `rgba(8,145,178,.9)`；③ `.mlo-value`/`.mlo-sep` 同步改深色系。验证门禁全过（lint:syntax 145/145、lint:imports OK、test:unit 111/111、typecheck OK）。commit `7231eca` 已提交；style.css 经 Contents API 镜像推送（remote blob `1450da8`）。
- **vinyl 歌词偏移工具条水平居中（用户截图要求"和歌词对齐居中"）**：`.ms-lyrics-bar` 全局为 `justify-content: flex-start`（左对齐），而歌词列在右栏内居中滚动，工具条偏左与歌词视觉中心不一致。修复（`dd71186`）：给 vinyl 样式单独加 `#music-stage.style-vinyl .ms-lyrics-bar { justify-content: center; }`，让 `-5s/-0.5s/+0.5s/+5s/译` 按钮组在歌词区底部水平居中，与上方居中对齐的歌词列保持同轴。验证门禁全过（lint:syntax 145/145、lint:imports OK、test:unit 111/111、typecheck OK）。commit `dd71186` 已提交；style.css 经 Contents API 镜像推送（remote blob `4421d24`）。
- **歌词优先样式背景改为清晰写真（初版按"专辑封面"实现）**：用户要求"把这个样式的背景改成专辑写真"，选中"歌词优先"即 `style-lyrics`，该样式此前沿用全局 `.ms-backdrop` 的 `filter: blur(120px) saturate(160%) brightness(.65)`，把同一张专辑封面图严重模糊+压暗。修复（`62c82e8`）：① `style-lyrics` 下 `.ms-backdrop` 去掉模糊滤镜（`filter: none`），并轻微放大 `scale(1.02)` 避免清晰图边缘露底色；② 叠加暗色渐变遮罩 `.ms-backdrop::after`（`rgba(0,0,0,.35) → .45 → .60`），保证白字歌词/标题可读；③ 关闭 `--ms-bottom-lift` 底部提亮层与 `.ms-ambient` 彩色光斑，避免与清晰写真冲突；④ 给标题/艺人/歌词/翻译/credits 统一加 `text-shadow: 0 2px 8px rgba(0,0,0,.45)`，进一步提升复杂封面上的可读性。commit `62c82e8` 已提交；style.css 经 Contents API 镜像推送（remote blob `deb1416`）。
- **修正"写真"语义：真正用艺人照片而非专辑封面（用户纠正"你这显示的不是写真吧？你是这是专辑吧"）**：`62c82e8` 那版只是把**专辑封面**去模糊清晰显示，但用户所指"写真"是**艺人照片/宣传照**。新增艺人写真功能（`5e79d04`）：① 新建 `src/main/ffmpeg/artist-photo.js`，策略为**本地目录优先**（`%APPDATA%/Lumora/artist-photos/<safe-artist>.jpg`）→ 未命中时调**网易云音乐艺人搜索 API**（`/api/search/get/web?type=100`）取 `picUrl/img1v1Url` 并下载、回写本地缓存（含 3xx 重定向递归、最小体积校验）；② `ipc-media.js` 暴露 `app:artist-photo` handler，`preload.js` 暴露 `window.lumen.getArtistPhoto(artist)`；③ `music-stage.js` 集成：`enterAudioMode` 在封面拉取后**异步**拉取艺人写真（沿用封面 token 防竞态），新增 `_artistPhotoUrl`/`_coverDataUrl` 双状态、`applyArtistPhoto()`、`_currentBackdropUrl()`（`style-lyrics` 且已获取写真时优先用艺人写真，否则回退专辑封面）、`_applyPlayerStyle` 切换样式时实时刷新背景、`exitAudioMode` 重置两状态。验证门禁全过（lint:syntax 146/146、lint:imports OK、test:unit 111/111、typecheck OK）。commit `5e79d04` 已提交；artist-photo.js→`a007c80`、ipc-media.js→`9093435`、preload.js→`0b53956`、music-stage.js→`1b6c7d2` 经 Contents API 镜像推送。注意：首次播放某艺人需联网从网易云下载写真（之后本地缓存，离线可用）；也可手动在 `%APPDATA%/Lumora/artist-photos/` 放 `<艺人名>.jpg` 优先使用。
- **修复歌词优先样式背景"先专辑封面再跳写真"（用户截图追问"为什么要先显示专辑图片再跳转写真？"）**：根因是专辑封面从本地音频文件抽取（毫秒级返回），写真要走网易云 HTTPS 下载（几百毫秒~几秒），两者并行时封面先到、写真后到，代码直接把后者覆盖前者，造成视觉跳转。修复（`4259004`）：① 新增 `_artistPhotoPending`/`_artistPhotoTimeout` 状态；② `applyCover` 在 `style-lyrics` 且写真仍在获取时，**不把专辑封面设为背景**，只更新中间封面小图，保持暗色渐变占位；③ 写真成功时 `applyArtistPhoto` 统一刷新背景，失败/超时时走 `_fallbackArtistPhotoBackdrop()` 回退到专辑封面；④ 加 2.5s 超时降级，避免网络卡顿时一直黑底；⑤ `exitAudioMode` 清理 pending/timeout。验证门禁全过（lint:syntax 146/146、lint:imports OK、test:unit 111/111、typecheck OK）。commit `4259004` 已提交；music-stage.js→`ba58bdc` 经 Contents API 镜像推送。
- **音频频谱改为多彩渐变（用户截图"音频频谱能不能不要只一种颜色"）**：原实现整段频谱共用单一纵向渐变（歌词底部为固定蓝系，主频谱为专辑强调色的深→中→亮），视觉上接近单色。修复（`d51f8b8`）：① 每根频柱独立渐变，纵向保留暗→亮景深，横向按频率索引做色相偏移；② 歌词底部频谱（`style-lyrics/lyrics-min`）改用 Lumora 品牌彩虹流：青 `#6ee7ff` → 蓝 `#4d8dff` → 紫 `#7c8cff` → 粉 `#ff7ac6`；③ 主频谱（其他样式）以专辑强调色 `_accentRGB` 为锚，左右各偏移 ±30°（左端偏冷/青，右端偏暖/粉）；④ 新增 `_rgbToHsl`/`_hslToRgb`/`_spectrumBarGradient` 辅助函数，绘制时按柱索引单独 `ctx.fill()`。验证门禁全过（lint:syntax 146/146、lint:imports OK、test:unit 111/111、typecheck OK）。commit `d51f8b8` 已提交；music-stage.js→`8b91b3d` 经 Contents API 镜像推送。
- **音频频谱改为真实 FFT 频谱（去对称拱形，用户"总感觉频谱起伏和节奏有点匹配"）**：原频谱虽是真实 FFT（`getByteFrequencyData`），但被 `smoothingTimeConstant=0.82` + 强制对称拱形 `0.30+0.70*sin` + 歌词底部镜像重排 + 慢 decay 抹平，导致"有点匹配但不真实"。修复（`4ff93ee`）：① 去掉 `_drawSpectrumBars` 的对称山形包络（`env` 恒为 1），柱高直接反映该频带能量（低频在左、高频在右）；② 频率映射由二次 `pow(i/N,2)` 改为对数 `exp(logMin+(i/N)*(logMax-logMin))`，每根柱覆盖约一个倍频程带，右侧不再被压空；③ 去掉 `0.30+0.70*sin` 对称弓形整形与 `_renderSpectrum` 的歌词底部"中心向外镜像重排"，歌词底部与主频谱一致按真实频序绘制；④ `AnalyserNode.smoothingTimeConstant` 由 `0.82` 降到 `0.5`，瞬态/鼓点不再被时间平均抹平；⑤ 峰值缓动加快——attack `0.16→0.30`、decay `0.022→0.035`，更跟拍不再"慢半拍"；空间平滑由 2 遍降为 1 遍 3-tap。验证门禁全过（lint:syntax 146/146、lint:imports OK、test:unit 111/111、typecheck OK）。commit `4ff93ee` 已提交；music-stage.js→`12669e7`、audio.js→`450b363` 经 Contents API 镜像推送。
- **主页 / 音乐播放器右键菜单关闭（用户：主页右键弹出菜单 + 音乐播放器右键菜单也关掉）**：`src/renderer/input.js` 的全局 `contextmenu` 监听器默认在播放区、OSC、落地页、音乐页等都弹出自定义右键菜单；但主页（idle 落地页）与音乐播放器为内容展示区，不需要右键菜单。修复（`dd06054` + `371f70a`）：把模态面板/可编辑区域的原生行为豁免提到最前，再对 `idle-mode` / `audio-mode` 直接 `e.preventDefault()` + return，既不弹自定义菜单也不显示原生菜单；豁免列表（设置/播放列表/字幕/投屏等面板 + `input/textarea/select`）内仍保留原生行为。验证门禁全过（lint:syntax 146/146、lint:imports OK、test:unit 111/111、typecheck OK）。commit `dd06054`（`45976ea`）+ `371f70a`（`37decd4`）已提交，input.js 经 Contents API 镜像推送。
- **歌词偏移工具条默认可见（用户问"歌词上色怎么不同步音乐"）**：歌词高亮不同步通常不是播放器时钟问题（音频时钟来自 `AudioWorklet`），而是**在线下载的 LRC 歌词时间戳与当前音频文件不匹配**。项目已有歌词偏移工具条（`-5s / -0.5s / +0.5s / +5s`），但在非 vinyl 暗色样式下默认 `opacity:0` 仅 hover 显示，用户难以发现。修复（`f610e51`）：把 `.ms-lyrics-wrap.has-lyrics .ms-lyrics-bar` 默认改为 `opacity:0.55` + `pointer-events:auto`（与 vinyl 处理一致），hover/focus 时 `opacity:1`，让用户能直接看到并点击校准；每首歌的偏移按路径持久化到 `localStorage`。验证门禁全过（lint:syntax 146/146、lint:imports OK、test:unit 111/111、typecheck OK）。commit `f610e51` 已提交；style.css→`4b8d2ce` 经 Contents API 镜像推送。注意：若所有歌曲都偏差同一固定值，可再加一个全局歌词延迟设置；目前先以每首校准为主。

- **歌词当前行"先变白高亮、再随进度从左上色"（用户截图纠正："我想要的是先变白高亮再上颜色"）**：原实现当前句未唱段是暗灰 `rgba(255,255,255,.40)`、已唱段是暖金，导致"直接上色、未唱段暗"。修复（`c0dd9d3`）：把 `.ms-lyric-line.active .ms-lyric-text` 渐变的未唱段（`--prog` 控制的分界之后）由暗灰改为纯白 `#fff`，使 `--prog=0` 时整句纯白高亮，随 `--prog` 增大左侧已唱部分刷上暖金、右侧待唱部分保持纯白——即"先整句变白高亮，再随演唱进度从左到右上色"。vinyl 浅色主题的未唱段由半透明深灰 `rgba(0,0,0,.60)` 改为 `#111827`（白→深），保持一致语义。验证门禁全过（lint:syntax 146/146、lint:imports OK、test:unit 111/111、typecheck OK）。commit `c0dd9d3` 已提交；style.css 经 Contents API 镜像推送（remote blob `73d3e7f`）。

- **歌词逐字着色（唱到哪个字就哪个字着色，commit `f6b7639`）**：用户要求"唱到哪个字就哪个字着色，不要整句唱完才一次性上色"。根因是普通 LRC 只有整行时间戳，原实现把整行进度 `--prog` 线性映射到每个字（假设匀速），真人演唱快慢不均时就像"整句一次性上色"。修复（接入真·逐字歌词源）：① `lyrics.js` 的 `parseLrc` 识别行内 `<mm:ss.xx>` 逐字时间戳（Musixmatch rich-sync，绝对时间基），为每行产出 `words`/`charTimes`（按字在词内均匀分配）；新增 Musixmatch 逐字歌词源（设置填 `music.lyrics-musixmatch-token` 后优先下载，含 `<>` 逐字标签），失败/留空回退 LRCLIB 行级。② `music-stage.js` 的 `_buildLyrics` 为每行算 `charTimes`（有 words 用精确值，否则按行跨度均匀估算）；`_applyLineState`/`_startLyricRAF` 改为按 `charTimes` 逐字切换 `.lit`（金色），不再用整行 `--prog` 渐变。③ `style.css` 当前行整句白色高亮、已唱到的字逐字变金（`--lyric-sung`）；vinyl/glass 浅色主题未唱深、已唱青绿。④ 新增 `test/unit/lyrics-word.test.js` 锁定逐字解析。验证门禁全过（lint:syntax 146/146、lint:imports OK、test:unit 114/114、typecheck OK）。commit `f6b7639`；blobs lyrics.js→`05386a6`、music-stage.js→`3a9badd`、style.css→`c0cbc6e`、config.js→`808b10c`、ipc-media.js→`f383e65`、settings.js→`9f011ae`、test→`6d804f0`。

- **歌词连续渐变扫光（"一点点着色"，commit `26519cf`）**：用户在逐字着色（`f6b7639`）后又推翻——"我想要那种一点点着色，不要那种一个字一个字的着色"。即逐字 `.lit` 跳变太离散，要连续液体式填充。修复：当前句高亮改回 CSS `background-clip:text` 渐变扫光，由 `--prog`(0~1) 驱动，像液体一样从左到右连续上暖金色（vinyl=青绿，glass=琥珀→深褐）；`--prog` 由新增 `_charTimesToProg(time,ct)` 基于逐字时间戳 `charTimes` 连续插值驱动，既连续无跳变又贴合真实字时间；普通 LRC 无逐字时间时退化为均匀行内扫光。清理旧逐字 `.lit` 残留，避免切主题冲突；glass 主题补全 active 行透明+渐变，与暗/vinyl 一致。验证门禁全过（lint:syntax 147/147、lint:imports OK、test:unit 114/114、typecheck OK）；pre-commit 通过。commit `26519cf`；Contents API 镜像 music-stage.js→`ffe71e4`、style.css→`e0dd3af`。

- **歌词扫光不再被静音间隙拖慢（"快播放完了才开始着色"，commit `dbeec8c`）**：用户反馈当前句"快播放完了才开始着色"。经比对确认渐变扫光与上一版逐字方案使用同一套 `charTimes`（仅渲染方式不同），时序逻辑无回归；根因是普通 LRC 无逐字时间戳时，每行着色跨度被铺满到"下一句之前"，若句间有静音间隙，gold 扫光会被拖到行末/下一句才完成。修复（`dbeec8c`）：`_estimateCharTimes` 与无 `charTimes` 兜底分支改用"约 0.30s/字（下限 0.8s）"的演唱时长估算，`--prog` 紧贴实际演唱进度；逐字 LRC（Musixmatch）不受影响。验证门禁全过（lint:syntax 147/147、test:unit 114/114、typecheck OK）；pre-commit 通过。commit `dbeec8c`；Contents API 镜像 music-stage.js→`635568a`。

- **歌词自动偏移校准（ffmpeg 检测起音，commit `79b1d8c`）**：用户选择"做自动偏移校准"。新增 `src/main/ffmpeg/lyrics-offset.js`：`bandpass(1kHz)+ebur128` 提取音频人声频段能量序列，`parseVocalOnset` 取"峰值-12LU"阈值下首个明显起音；`computeLyricAutoOffset` 由"起音时间 - LRC 首行时间戳"算偏移（钳制 [-10,+30]，误检返回 null）。IPC `app:lyric-auto-offset`（preload 暴露 `window.lumen.lyricAutoOffset`）由主进程跑 ffmpeg。渲染端：歌词首加载且本曲目未校准过时自动计算并应用，结果按曲目缓存（localStorage）；手动 `-0.5s/+0.5s` 修正后标记"已接管"不再覆盖；歌词区「自动」按钮可重新校准。最佳努力：带长伴奏前奏的歌可能把前奏误判为人声（偏移偏早），仍可用手动工具条修正。验证门禁全过（lint:syntax 149/149、test:unit 118/118、typecheck OK）；pre-commit 通过。commit `79b1d8c`；Contents API 镜像 lyrics-offset.js→`b7fd96f`、ipc-media.js→`80008e6`、preload.js→`226d609`、music-stage.js→`aa4a120`、index.html→`13db634`、test→`de11778`。注：若整句仍整体偏晚（固定延迟），属 LRC 与音频偏移，用歌词区底部 `-0.5s/+0.5s` 工具条按曲目修正。

- **修复歌词扫光节奏乱（渐变版偏移符号写反，commit `ca26c82`）**：用户反馈"着色节奏还是乱的"。自动偏移校准（`79b1d8c`）与手动设非零偏移后，当前句一激活整行瞬间满金、节奏全乱。根因是渐变扫光版 `--prog` 计算把偏移符号写反——用了 `time + off`，而激活判断（`time >= ln.time + off`）与逐字版（`time >= ct[i] + off`）均用 `+off`。非零偏移下激活瞬间 `time + off` 远超所有字时间戳，`_charTimesToProg` 直接返回 1，整行满金、节奏乱。修复（`ca26c82`）：`_applyLineState`（L1822/1825）与 `_startLyricRAF`（L1855/1858）的 `--prog` 计算统一改为 `time - off` / `t - off`，与激活逻辑自洽——激活仍按 `+off` 判定，着色推进按 `-off` 反算真实字相对时刻。验证门禁全过（lint:syntax 149/149、test:unit 118/118、typecheck OK）；pre-commit 通过。commit `ca26c82`；Contents API 镜像 music-stage.js→`a0a36f7`。

- **隐藏歌词区 credits 元数据行（用户截图"作曲 Mirko Limoni"红框，commit `84fcb58`）**：歌词区上方会显示作词/作曲/编曲/制作人/和声/混音等 credits 元数据行，用户要求删掉。修复：在 `style.css` 加 `#m-lyrics-credits { display: none !important; }` 全局隐藏该块；保留 `music-stage.js` 的查询与构建逻辑不动，方便以后若要恢复可一键撤销。验证门禁全过（lint:syntax 153/153、lint:imports OK、test:unit 118/118、typecheck OK）；pre-commit 通过。commit `84fcb58`；Contents API 镜像 style.css→`2d56ef0`。

- **修复简约歌词长歌词被裁掉（用户截图"光年之外（《太空旅客（Passengers）》电影中国区主题曲）- G.E.M. 邓紫棋"溢出红框，commit `84eaa96`）**：简约歌词样式（`style-lyrics-min`）歌词居中放大，`.ms-lyrics-wrap` 有 `overflow:hidden`，超长歌词（中英混排、带括号标题）被横向裁掉。修复：给 `#music-stage.style-lyrics-min .ms-lyric-text` 加 `white-space: normal; overflow-wrap: break-word; word-break: break-word; hyphens: auto;`，使超长歌词自动换行显示完整，不再被截断。验证门禁全过（lint:syntax 153/153、lint:imports OK、test:unit 118/118、typecheck OK）；pre-commit 通过。commit `84eaa96`；Contents API 镜像 style.css→`eda4179`。

- **排查其他播放样式同类问题 + 统一换行到基础类（commit `3656950`）**：用户要求检查另三个样式（style-lyrics / style-vinyl / style-glass）是否同样会裁长歌词。结论：三者 `.ms-lyric-text` 继承基础类，默认 `white-space:normal` 已能换行 CJK/空格文本，但**缺 `overflow-wrap/word-break:break-word`**，长英文无空格标题仍可能被 `.ms-lyrics-wrap` 的 `overflow:hidden` 横向裁掉。修复：把换行规则上提到共享基础 `.ms-lyric-text`（L4218），并移除 `style-lyrics-min` 中已冗余的副本，四个样式统一由基础类兜底。验证门禁全过（lint:syntax 153/153、lint:imports OK、test:unit 118/118、typecheck OK）；pre-commit 通过。commit `3656950`；Contents API 镜像 style.css→`d1317e0`。

- **底部控制条歌曲名跟随播放/暂停按钮颜色（commit `5b2f4aa`）**：用户截图指出左下角歌曲名应使用播放/暂停按钮颜色，且每个播放样式都生效。`.mc-track-title` 原本固定色（暗色主题 `#fff`、vinyl `#101820`、浅色控制条 `#1a1a1a`），改为 `var(--style-accent)`，与播放按钮、进度条共用 `body.music-style-*` 定义的各样式主题色：default 紫 `#7c8cff`、cover 专辑主色、lyrics 青 `#6ee7ff`、minimal 紫 `#a78bfa`、vinyl 橙 `#ff9f43`、square 青绿 `#4ecdc4`、glass 浅蓝 `#93c5fd`、lyrics-min 粉 `#ff7ac6`。验证门禁全过（lint:syntax 153/153、lint:imports OK、test:unit 118/118、typecheck OK）；pre-commit 通过。commit `5b2f4aa`；Contents API 镜像 style.css→`8667f6a`。

- **底部控制条艺人名、分隔符也随歌曲名一起上色（commit `0ff973e`）**：用户反馈曲目信息里只有歌名 `倒数` 是主题色，后面的 ` - G.E.M.邓紫棋` 仍是灰/暗色，看起来像只有一半上色。修复：把 `.mc-track-sep`、`.mc-track-artist` 也改为 `var(--style-accent)`，并设 `opacity:.75` 让艺人名略淡于歌名，保持层级；同时更新 `body.music-style-vinyl` 与 `#music-controls.mc-theme-light` 的覆盖，确保各播放样式下一整行都统一着色。验证门禁全过（lint:syntax 153/153、lint:imports OK、test:unit 118/118、typecheck OK）；pre-commit 通过。commit `0ff973e`；Contents API 镜像 style.css→`7ba5fe5`。 

- **下载歌词自动补全词/曲/编曲等到 LRC（commit `a71a774`）**：用户希望所有歌曲下载的歌词都带制作信息（而非仅部分源自带）。改动：`downloadLyrics` 在保存 `.lrc` 前，调用现有 `queryCredits`（MusicBrainz + 网易云）补全 词/曲/编曲/制作人/和声/混音，以 `[00:00.00]词：xxx` 形式并入 LRC 头部；LRC 已自带 credits 时不重复，查询失败静默降级为纯歌词。新增配置 `music.lyrics-include-credits`（默认 yes，可在设置关闭），由 `ipc-media` 读取传入。渲染端不变：这些行作为歌词显示，且 `_hasCreditLines` 命中后自动隐藏独立 credits 块、不重复。验证门禁全过（lint:syntax 153/153、lint:imports OK、typecheck OK）；pre-commit 通过。commit `a71a774`；Contents API 镜像 config.js→`f218cbc`、lyrics.js→`6cb4829`、ipc-media.js→`405d193`。

- **所有播放样式都显示歌词区与偏移/自动/翻译工具（commit `13628e0` / `ec526f0`）**：用户截图里只有部分样式底部有歌词偏移校准/自动/翻译工具条，询问为什么不是每个样式都有。根因：`style-default`、`style-cover` 默认 `display:none` 隐藏 `.ms-lyrics-wrap`；`style-minimal` 直接隐藏整个 `.ms-right`。修复：① 取消这三个样式的隐藏，分别给 `.ms-right` 设置合理宽度与 `max-height`（default 42vh/420px、cover 34vh/320px、minimal 30vh/280px），并让歌词/歌词行/歌词文本居中对齐，与各自样式氛围一致；② 用户后续截图仍有样式看不到，发现 `.ms-lyrics-bar` 默认 `opacity:.55`、hover 才全亮，在未 hover 或浅色背景下几乎不可见，改为默认 `opacity:1` 全亮。现在 default、cover、minimal 及其他所有有歌词的样式都会在歌词下方直接显示 `-5s/-0.5s/+0.5s/+5s/自动/译` 工具条。验证门禁全过（lint:syntax 153/153、lint:imports OK、test:unit 118/118、typecheck OK）；pre-commit 通过。commit `13628e0` / `ec526f0`；Contents API 镜像 style.css→`4d8f590`。

- **音频频谱与当前播放样式播放/暂停按钮颜色同步（commit `c16217e`）**：用户截图里歌词优先（lyrics-min）样式下频谱仍是蓝色，而播放按钮为粉色，要求频谱也跟随样式主题色。改动：新增 `_getStyleAccentRGB()` 读取 `body` 上的 CSS `--style-accent`（即播放/暂停按钮、进度条、歌曲名共用的颜色），失败时回退专辑强调色；`_spectrumBarGradient` 统一以该色为锚，左右做 ±25° 色相偏移，保留流动渐变；`_drawSpectrumBars` 的阴影/光晕也改用同色系。覆盖主频谱（封面底部）和歌词底部频谱。各样式现在会按对应主题色显示：default 紫 `#7c8cff`、cover 专辑主色、lyrics 青 `#6ee7ff`、minimal 紫 `#a78bfa`、vinyl 橙 `#ff9f43`、square 青绿 `#4ecdc4`、glass 浅蓝 `#93c5fd`、lyrics-min 粉 `#ff7ac6`。验证门禁全过（lint:syntax 153/153、lint:imports OK、test:unit 118/118、typecheck OK）；pre-commit 通过。commit `c16217e`；Contents API 镜像 music-stage.js→`21b05f8`。

- **音频频谱没有起伏时不显示（commit `05fd771`）**：用户截图里歌曲高频段仍有一排 tiny bars，要求“没有起伏的时候就不显示，只有有起伏才显示”。改动：① 播放中 FFT 目标计算去掉 `+0.02` 的底噪补偿，静音时能量真正归零；② 暂停/未播放时目标从 `0.1` 改为 `0`，峰值自然衰减；③ 峰值最低钳制从 `0.04` 改为 `0`，允许完全归零；④ `_drawSpectrumBars` 增加单柱可见阈值 `max(3px, 3.5% 高度)`，能量过低的高频柱/底噪柱直接跳过不画。效果：暂停、静音、歌曲间空白、以及高频自然衰减到无能量的频段，频谱会完全消失；音乐响起时重新出现。验证门禁全过（lint:syntax 153/153、lint:imports OK、test:unit 118/118、typecheck OK）；pre-commit 通过。commit `05fd771`；Contents API 镜像 music-stage.js→`5b317a8`。

- **确认下载的歌词都带时间轴 + 双保险（commit `4a6b5f1`）**：用户希望确认“每次下载的歌词都加上时间轴、可被播放器读取并匹配”。代码核查结论：`downloadLyrics` 只会落盘 LRCLIB 的 `syncedLyrics`（含 `[mm:ss.xx]`）或 Musixmatch 逐字 LRC（含 `< >` 行内时间），**不存在纯文本落盘路径**；本地若有无时间轴的 `.lrc`，只可能来自其它软件下载的旧文件。加固：① Musixmatch 分支仅当解析出至少一行 `time>0` 的歌词才保存并 return，否则继续走 LRCLIB 兜底；② LRCLIB 分支保存前新增 `lines.some(time>0)` 校验，无时间轴则拒绝落盘返回 `ok:false`。③ 新增 `test/unit/lyrics-synced-guarantee.test.js`：mock LRCLIB 返回带 `syncedLyrics` 时断言落盘 `.lrc` 每行都有有效时间轴；仅返回纯文本时断言拒绝落盘，锁死该不变量。验证门禁全过（lint:syntax 154/154、lint:imports OK、test:unit 120/120、typecheck OK）；pre-commit 通过。commit `4a6b5f1`；Contents API 镜像 lyrics.js→`d1c9f14`、测试→`1112098`。

- **credits 行跟随时间顺序着色（不单独提取，commit `74519f6`）**：用户反馈播放时不是从歌词第一个字开始着色。根因：下载写入的 credits 行（曲/编曲/制作人/和声）时间戳全为 `[00:00.00]`，`parseLrc` 把它们当作普通歌词行加入 `_lyricLines`；激活索引 `_lyricLines[i].time+offset<=time` 在开头会把 `idx` 累加到最后一个 credits 行，使其成了“当前句”被逐字扫光，真正第一句歌词被挤到后面不亮。用户诉求「不单独提出来，但也要跟着时间顺序着色」：① `lyrics.js parseLrc` 给 credits 行标记 `isCredit`，解析后若其全部堆在 `[00:00]` 且第一句真实歌词时间 >0，则给这些前置 credits 行分配递增时间戳（在 `[0, 第一句歌词)` 均匀分布），使其像普通歌词随播放时间顺序依次着色、自然过渡到第一句歌词；② 渲染端 `_hasCreditLines` 正则补充 `词/曲` 别名，确保生成的 credits 行稳定命中“LRC 已自带”，彻底不显示重复 credits 块。验证门禁全过（lint:syntax 154/154、lint:imports OK、test:unit 120/120、typecheck OK）；pre-commit 通过。commit `74519f6`；Contents API 镜像 lyrics.js→`381f70c`、music-stage.js→`7053127`。

- **音量条颜色与播放/暂停按钮同步（commit `3c950ae`）**：用户截图里音量滑块仍是绿色，而播放/暂停按钮为粉色，要求音量控件颜色跟随当前播放样式主题色。改动：`src/renderer/style.css` 中 `#m-volume-fill`、`#m-volume-handle`（音乐模式竖向音量）、`#volume-fill`、`#volume-handle`（通用横向音量）、浅色主题覆盖 `#music-controls.mc-theme-light #m-volume-fill/handle` 以及 `#m-volume:focus-visible` 的焦点环，全部从固定绿/渐变/黑白改为 `var(--style-accent, var(--accent, #7c8cff))`；并给 fill 与 handle 加同色系 `color-mix` glow。这样音量滑块会与播放/暂停按钮、进度条、频谱、歌曲名保持同一主题色，切到不同播放样式时同步变色。验证门禁全过（lint:syntax 154/154、lint:imports OK、test:unit 120/120、typecheck OK）；pre-commit 通过。commit `3c950ae`；Contents API 镜像 style.css→`47dc844`。
