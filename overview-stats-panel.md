# 统计面板 / 媒体信息 UI（v1+v2 融合 + 全中文化）

## 融合思路
- **v1（系统检查器卡片风）**：分区标题带渐变竖条、两列网格对齐、厚玻璃卡片、中文「媒体信息」。
- **v2（mpv 终端风）**：密集等宽、内联 `label: value`、节标题与主值同行、子字段缩进、一行多字段、560px 宽。
- **最终版（融合）**：取 v2 的密集内联布局结构，吸收 v1 的质感——卡片化面板 + 分区标题用品牌色强调；全部标签/标题中文化（专有名词 FFmpeg / Electron / Chromium / MaxCLL / SAR / A-V 保留英文）。

## 改动文件
- `src/renderer/ui/stats.js` — 数据渲染（sections 结构 + 中文标签）
- `src/renderer/style.css` — 统计面板样式

## 布局要点
- 节标题与主值同行：`视频：H.264 10bit`；子字段缩进 18px 排在下方
- 一行可并列多字段：`帧率：23.976 fps   分辨率：1920×1080（1.78:1）`
- 分区：文件 / 显示（A-V、丢帧、主时钟）/ 帧时间（末/均/峰值 ms）/ 视频 / 音频 / 渲染管线 / 传输 / 环境
- 头部「媒体信息 / 按 i 关闭」，sticky 吸顶，刷新时保留滚动位置
- 状态色：`good #5ee6a8` / `warn #ffcf6a` / `bad #ff8585`（A-V 偏差、欠载、缓冲）

## 验证
- `npm run lint:imports` 通过
- `npm run test:unit` 57/57 通过
- `npm run typecheck` 通过

## 生效方式
重启播放器后按 `i` 键查看。改动未提交，与缩略图 / 右键菜单 / 投屏面板等改动一起堆在本地。
